enum QueryOperationTypes {
    EQUALS = 'equals',
    NOT_EQUALS = 'not_equals',
    CONTAINS = 'contains',
    IN = 'in',
    NOT_IN = 'not_in',
    ARRAY_CONTAINS = 'array_contains'
}

export default QueryOperationTypes;